#:import win kivy.core.window.Window
<RootWidget>
    bcolor: 0,0,0,1
    GridLayout:
        Label:
            id: lense_text_id
            text: root.main_label
            text_size: self.size
            halign: 'right'
            valign: 'middle'
            bold: True
            pos: 130, 150
            color: 125, 5, 225, 1
